# Ядро AutoStars: python -m autostars.main
# Ставится одной командой: pip install -r requirements.txt
#
# GUI (flet) и инструменты — отдельные extra:
#   pip install -e ".[gui]"      # настольный интерфейс
#   pip install -e ".[dev]"      # pytest + ruff + flet
# Зависимости legacy-кода — в legacy/requirements.txt (он не поддерживается).
httpx>=0.27,<1
aiosqlite>=0.20,<1
beautifulsoup4>=4.12,<5
